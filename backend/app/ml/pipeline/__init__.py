"""Phase 2: Data pipeline & feature engineering.

This package provides a CLI-driven, dataset-agnostic preprocessing + feature
extraction pipeline.

Outputs:
- Parquet splits (train/val/test)
- artifacts (preprocessors, feature schema)
- metadata & reports (dataset statistics, missingness, encoding/scaling info)
"""

